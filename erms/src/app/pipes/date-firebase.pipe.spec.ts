import { DateFirebasePipe } from './date-firebase.pipe';

describe('DateFirebasePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFirebasePipe();
    expect(pipe).toBeTruthy();
  });
});
